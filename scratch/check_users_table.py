import os, psycopg2; from dotenv import load_dotenv; load_dotenv('web_app/backend/.env'); conn = psycopg2.connect(os.getenv('DATABASE_URL')); cur = conn.cursor(); cur.execute("SELECT column_name FROM information_schema.columns WHERE table_name = 'users'"); print(cur.fetchall()); cur.close(); conn.close()